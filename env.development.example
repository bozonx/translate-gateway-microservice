###### Basic app settings
NODE_ENV=development
LISTEN_HOST=0.0.0.0
LISTEN_PORT=8080
API_BASE_PATH=api
LOG_LEVEL=warn
# Force application timezone (affects pretty logs)
TZ=UTC

###### HTTP
# Max HTTP request body size (MB)
HTTP_REQUEST_BODY_LIMIT_MB=10

###### Translation service
# Max allowed length of input text (characters)
TRANSLATE_MAX_TEXT_LENGTH=5000
# Default translation provider if not specified in request
TRANSLATE_DEFAULT_PROVIDER=google
# Comma-separated allow-list of providers. If empty, all providers are allowed
TRANSLATE_ALLOWED_PROVIDERS=
# Request timeout for provider calls (seconds)
REQUEST_TIMEOUT_SEC=60
# System prompt template for translation (placeholders: {targetLang}, {sourceLang}, {format})
TRANSLATE_LLM_SYSTEM_PROMPT=You are a professional translator. Translate the user message into {targetLang}. If source language is provided ({sourceLang}), use it; otherwise detect it. Preserve meaning, tone, and formatting. For format={format}, keep the same formatting (preserve HTML tags when html). Output only the translated text, without any explanations.

# Google Cloud credentials for Translate API
# The SDK uses ADC (Application Default Credentials).
# Example (path inside container/host):
# GOOGLE_APPLICATION_CREDENTIALS=/secrets/gcp-service-account.json

# DeepL credentials
# API key for DeepL API (Free or Pro)
# Example:
# DEEPL_AUTH_KEY=xxxxxxxx-xxxx-xxxx-xxxx-xxxxxxxxxxxx:fx
# DEEPL_AUTH_KEY=

# DeepSeek LLM (OpenAI-compatible)
# API key for DeepSeek
# DEEPSEEK_API_KEY=
# Base URL for DeepSeek API (override if needed)
# DEEPSEEK_API_BASE_URL=https://api.deepseek.com
# Default model name if not provided in the request
# DEEPSEEK_DEFAULT_MODEL=deepseek-chat

# OpenRouter LLM (OpenAI-compatible)
# API key for OpenRouter
# OPENROUTER_API_KEY=
# Base URL for OpenRouter API (override if needed)
# OPENROUTER_API_BASE_URL=https://openrouter.ai/api/v1
# Default model name if not provided in the request
# OPENROUTER_DEFAULT_MODEL=openrouter/auto
